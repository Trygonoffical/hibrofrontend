/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['127.0.0.1', 'localhost' , 'hibroapi.trygon.tech'],
        // Add your production domain when deploying
      },
};

export default nextConfig;
